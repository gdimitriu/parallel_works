# Generated by makegen....

TARGET =gauss-fast
OBJECTS = gauss-parallel-fast.o bench.o\
	pthread_barrier_sem.o pthread_barrier.o pthread_semaphores.o

CC = gcc
CFLAGS =-O3 $(INCDIR) -Wall

LIBS =
LIBDIR =
INCDIR = -I/usr/include -I./

INSTALLDIR = 

all: clean 	$(OBJECTS)
	$(CC) -o $(TARGET)  $(OBJECTS) -lpthread

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS)

