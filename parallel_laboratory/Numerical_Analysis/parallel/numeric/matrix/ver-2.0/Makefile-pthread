# Generated by makegen....

TARGET = bench-pthread
OBJECTS =bench.o

CC = gcc
CFLAGS =  $(INCDIR) -O  -Wall
#LINUX
LIBS =  -lparallel-pthread -lpthread -lm -lz
LIBDIR =  -L../../shared/lib
INCDIR =  -I../../shared/include
#for SUNOS4
#LIBS =  -lparallel-pthread -lthread -lm -lposix4

INSTALLDIR = 

all: clean $(TARGET)

$(TARGET):$(OBJECTS)
	$(CC) $(CFLAGS) -o $(TARGET) $(OBJECTS) $(LIBDIR) $(LIBS)
	gcc -O -Wall -o generator generator.c -lm -lz

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS) generator

