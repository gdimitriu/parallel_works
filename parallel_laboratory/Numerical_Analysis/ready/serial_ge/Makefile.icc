# Generated by makegen....

TARGET =gauss-icc
OBJECTS =serial.o gauss-serial.o

CC = icc
CFLAGS =-O3 -mcpu=pentiumpro -tpp6 $(INCDIR) -Wall

LIBS =-lm
LIBDIR =
INCDIR = -I/usr/include -I./

INSTALLDIR = 

all: clean 	$(OBJECTS)
	$(CC) $(CFLAGS) -o $(TARGET)  $(OBJECTS) $(LIBS) $(LIBDIR)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) *.o

