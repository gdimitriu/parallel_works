# Generated by makegen....

TARGET = dynamic
OBJECTS = manager.o worker.o

CC = mpicc
CFLAGS =   $(INCDIR)

LIBS =  
LIBDIR =  
INCDIR =  

INSTALLDIR = 

all:clean  $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $(CFLAGS) -o worker worker.o $(LIBDIR) $(LIBS)
	$(CC) $(CFLAGS) -o manager manager.o $(LIBDIR) $(LIBS)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS) manager worker

