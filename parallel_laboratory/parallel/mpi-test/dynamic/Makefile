# Generated by makegen....

TARGET = dynamic
OBJECTS = client.o server.o

CC = mpicc
CFLAGS =   $(INCDIR)

LIBS =  
LIBDIR =  
INCDIR =  

INSTALLDIR = 

all:clean  $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $(CFLAGS) -o client client.o $(LIBDIR) $(LIBS)
	$(CC) $(CFLAGS) -o server server.o $(LIBDIR) $(LIBS)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS) server client

