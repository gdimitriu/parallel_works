# Generated by makegen....

TARGET =jacobi-omni
OBJECTS = bench-omp.o

CC = omcc
CFLAGS =-O3  $(INCDIR) -Wall

LIBS =-lpthread -lm
LIBDIR =
INCDIR = -I/usr/include -I./

INSTALLDIR = 

all: clean 	$(OBJECTS)
	$(CC) -o $(TARGET)  $(OBJECTS) $(LIBS) $(LIBDIR)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) *.o

