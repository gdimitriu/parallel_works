# Generated by makegen....

TARGET =gauss gaussd
OBJECTS = gauss.o gaussd.o generator.o

CC = gcc
CFLAGS =  -Wall $(INCDIR)

LIBS = -lm
LIBDIR = -L/usr/lib
INCDIR = -I/usr/include -I/opt/include

INSTALLDIR = 

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $(CFLAGS) -o gauss gauss.o $(LIBDIR) $(LIBS)
	$(CC) $(CFLAGS) -o gaussd gaussd.o $(LIBDIR) $(LIBS)
	$(CC) $(CFLAGS) -o generator generator.o $(LIBDIR) $(LIBS)
	strip gauss gaussd generator

clean:
	rm -f a.out core *~ \#* *.bak gauss gaussd generator $(OBJECTS)

