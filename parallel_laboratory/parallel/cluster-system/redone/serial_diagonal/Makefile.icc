# Generated by makegen....

TARGET =diag-icc
OBJECTS =bench-ser.o

CC = icc
CFLAGS =-O3 -mcpu=pentiumpro -tpp6 $(INCDIR) -Wall

LIBS =
LIBDIR =
INCDIR = -I/usr/include -I./

INSTALLDIR = 

all: clean 	$(OBJECTS)
	$(CC) $(CFLAGS) -o $(TARGET)  $(OBJECTS) -lpthread

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) *.o

