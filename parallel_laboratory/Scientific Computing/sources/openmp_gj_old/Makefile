# Generated by makegen....

TARGET =gaussJ-omni
OBJECTS = bench.o gaussJ-omp-fast.o

CC = omcc
CFLAGS =-O3  $(INCDIR) -Wall

LIBS =-lpthread
LIBDIR =
INCDIR = -I/usr/include -I./

INSTALLDIR = 

all: clean 	$(OBJECTS)
	$(CC) -o $(TARGET)  $(OBJECTS) $(LIBS)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) *.o

