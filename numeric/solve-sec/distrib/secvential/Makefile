# Generated by makegen....

TARGET = gauss
OBJECTS = gauss.o

CC = gcc
CFLAGS = -Wall $(INCDIR)

LIBS =  
LIBDIR =  
INCDIR =  

INSTALLDIR = 

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $(CFLAGS) -o $(TARGET) $(OBJECTS) $(LIBDIR) $(LIBS)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS)

