# Generated by makegen....

TARGET = gauss-pthread
OBJECTS =

CC = gcc
CFLAGS =  $(INCDIR) -O # -Wall
#LINUX
LIBS =  -lparallel-pthread -lpthread -lm -lz
LIBDIR =  -L../../shared/lib
INCDIR =  -I../../shared/include
#for SUNOS4
#LIBS =  -lparallel-pthread -lthread -lm -lposix4

INSTALLDIR = 

all: clean $(TARGET)

$(TARGET):
	$(CC) $(CFLAGS) -o gauss-pthread gauss-mp.c $(LIBDIR) $(LIBS)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) $(OBJECTS)

