# Generated by makegen....

TARGET =libserialnum
OBJECTS = calcul-gauss.o

CC = gcc
CFLAGS =-O $(INCDIR) -Wall

LIBS =
LIBDIR =
INCDIR = -I/usr/include -I../include

INSTALLDIR = 
all: clean exec

exec:	$(OBJECTS)
	ar -vq ../lib/libserialnum.a $(OBJECTS)
	ranlib ../lib/libserialnum.a

clean:
	rm -f a.out core *~ \#* *.bak ../lib/libseialnum.a $(OBJECTS)

