# Generated by makegen....

TARGET =gj-icc
OBJECTS =bench.o

CC = icc
CFLAGS =-O3 -mcpu=pentiumpro -tpp6 -openmp $(INCDIR) -Wall

LIBS =-lm
LIBDIR =
INCDIR = -I/usr/include -I./

INSTALLDIR = 

all: clean 	$(OBJECTS)
	$(CC) $(CFLAGS) -o $(TARGET)  $(OBJECTS) $(LIBS) $(LIBDIR)

clean:
	rm -f a.out core *~ \#* *.bak $(TARGET) *.o

